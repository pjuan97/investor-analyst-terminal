-- Init script for Investor Analyst Terminal
-- This runs automatically when the container is first created

-- Enable UUID extension
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Create schema for better organization (optional, Prisma uses public by default)
-- CREATE SCHEMA IF NOT EXISTS investor;
